'use client';import { useState } from 'react';
export default function TransactionForm({items,warehouses}:{items:any[],warehouses:any[]}){const [msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();setMsg('');const fd=new FormData(e.currentTarget);const body=Object.fromEntries(fd.entries());const r=await fetch('/api/transactions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(r.ok){setMsg('Transaksi tersimpan.');e.currentTarget.reset();setTimeout(()=>location.reload(),500)}else setMsg('Gagal menyimpan transaksi.')}return <form className="form" onSubmit={submit}><h3>Input transaksi</h3>{msg&&<div className="notice success">{msg}</div>}<input className="input" name="transactionAt" type="date" required/><select className="input" name="type" required><option value="INBOUND">Barang Masuk</option><option value="OUTBOUND">Barang Keluar</option><option value="TRANSFER">Mutasi Gudang</option><option value="PRODUCTION">Produksi</option><option value="ADJUSTMENT">Adjustment</option></select><input className="input" name="itemCode" placeholder="Kode barang" list="items" required/><datalist id="items">{items.map(i=><option key={i.id} value={i.code}>{i.name}</option>)}</datalist><input className="input" name="itemName" placeholder="Nama barang (dipakai jika item baru)"/><input className="input" name="hsCode" placeholder="HS Code (opsional)"/><input className="input" name="unit" placeholder="Satuan" defaultValue="PCS"/><select className="input" name="warehouseCode">{warehouses.map(w=><option key={w.id} value={w.code}>{w.code} - {w.name}</option>)}</select><input className="input" name="qty" type="number" step="0.0001" min="0" placeholder="Qty" required/><input className="input" name="referenceNo" placeholder="No referensi / faktur"/><input className="input" name="customsDocNo" placeholder="No dokumen kepabeanan (opsional)"/><button className="btn">Simpan Transaksi</button></form>}
