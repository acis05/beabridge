'use client';import { useState } from 'react';
export default function UserManager({users,isAdmin}:{users:any[],isAdmin:boolean}){const [msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();const fd=new FormData(e.currentTarget);const r=await fetch('/api/users',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(fd.entries()))});const d=await r.json();setMsg(r.ok?'User dibuat.':d.error||'Gagal');if(r.ok)setTimeout(()=>location.reload(),500)}return <>{isAdmin&&<form className="row" onSubmit={submit} style={{marginBottom:18}}><input className="input" style={{maxWidth:180}} name="name" placeholder="Nama" required/><input className="input" style={{maxWidth:220}} name="email" type="email" placeholder="Email" required/><input className="input" style={{maxWidth:180}} name="password" type="password" placeholder="Password awal" minLength={8} required/><select className="input" style={{maxWidth:180}} name="role"><option>OPERATOR</option><option>SUPERVISOR</option><option>CUSTOMS</option><option>AUDITOR</option><option>ADMIN</option></select><button className="btn">Tambah User</button></form>}{msg&&<div className="notice">{msg}</div>}<table className="table"><thead><tr><th>Nama</th><th>Email</th><th>Role</th><th>Dibuat</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td><span className="badge">{u.role}</span></td><td>{new Date(u.createdAt).toLocaleString('id-ID')}</td></tr>)}</tbody></table></>}
