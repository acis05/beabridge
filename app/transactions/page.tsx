import AppShell from '@/components/AppShell';import TransactionForm from '@/components/TransactionForm';import { getCurrentUser } from '@/lib/auth';import { prisma } from '@/lib/prisma';
export default async function Page(){const u=await getCurrentUser();if(!u)return null;const [items,warehouses,txs]=await Promise.all([prisma.item.findMany({where:{companyId:u.companyId},orderBy:{code:'asc'}}),prisma.warehouse.findMany({where:{companyId:u.companyId}}),prisma.transaction.findMany({where:{companyId:u.companyId},include:{item:true,warehouse:true},orderBy:{transactionAt:'desc'},take:50})]);return <AppShell><h1 className="section-title">Transaksi Manual</h1><div className="grid2"><div className="card"><TransactionForm items={items} warehouses={warehouses}/></div><div className="card"><h3>Catatan</h3><p className="muted">Jika kode barang belum ada, buat otomatis dari form. Gudang default tersedia sebagai MAIN.</p></div></div><div className="card" style={{marginTop:16}}><table className="table"><thead><tr><th>Tanggal</th><th>Kode</th><th>Jenis</th><th>Qty</th><th>Referensi</th><th>Sumber</th></tr></thead><tbody>{txs.map(t=><tr key={t.id}><td>{t.transactionAt.toISOString().slice(0,10)}</td><td>{t.item.code}</td><td>{t.type}</td><td>{String(t.qty)}</td><td>{t.referenceNo||'-'}</td><td>{t.source}</td></tr>)}</tbody></table></div></AppShell>}
