import AppShell from '@/components/AppShell';import { getCurrentUser } from '@/lib/auth';import { prisma } from '@/lib/prisma';import UserManager from '@/components/UserManager';
export default async function Page(){const u=await getCurrentUser();if(!u)return null;const users=await prisma.user.findMany({where:{companyId:u.companyId},select:{id:true,name:true,email:true,role:true,createdAt:true},orderBy:{createdAt:'asc'}});return <AppShell><h1 className="section-title">Pengguna & Hak Akses</h1><div className="card"><UserManager users={users} isAdmin={u.role==='ADMIN'}/></div></AppShell>}
